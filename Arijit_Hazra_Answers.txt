Student --> Name, Category, Score

UPDATE Student SET Category = 'A' WHEN  Score >50 AND Category='B' WHEN Score<50


'''
# Sample code to perform I/O:

name = input()                  # Reading input from STDIN
print('Hi, %s.' % name)         # Writing output to STDOUT

# Warning: Printing unwanted or ill-formatted data to output will cause the test cases to fail
'''

# Write your code here
name = input()   
length_string= len(name)


midway= len(name)//2

for i in range(0,midway):
    left= ''.join(name(i))

for j in range(midway,len(name)):
    right=''.join(name(j))

print(str.concat(left,right))
